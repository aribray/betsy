<section class="main-body">
<h2>Congratulations! You are now a proud scam participant.</h2>

<section class="order-summary">

  <h4>Order Summary</h4>

  <h4>My Products</h4>

  <table class="table order-details">
    <thead>
      <tr>
        <th>Product</th>
        <th>Price</th>
        <th>Quantity</th>
        <th>Item Cost</th>
      </tr>
    </thead>
    <tbody>
      <% @order.orderitems.each do |item| %>
      <tr>
        <td>
          <%= link_to item.product.name, product_path(item.product)  %>
        </td>
        <td>
          $<%=item.product.price %>
        </td>
        <td>
          <%= item.quantity %>
        </td>
        <td>
          <p>$<%= item.item_total %></p>
        </td>
      </tr>
      <% end %>
    </tbody>
  </table>

  <table class="table order-info">
    <thead>
      <tr>
        <th>Taxes</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>
          <p>$<%= @order.taxes %></p>
        </td>
      </tr>
    </tbody>
  </table>

  <table class="table order-info">
    <thead>
      <tr>
        <th>Order Total Amount</th>
        <th>Purchase Date</th>
        <th>Current Status</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>
          <p>$<%= @order.total %></p>
        </td>
        <td>
          <p><%= @order.updated_at.to_date %></p>
        </td>
        <td>
          <%= @order.status.capitalize %>
        </td>
      </tr>
    </tbody>
  </table>

  </section>

<nav>
  <span>
    <%=link_to "Cancel this order", order_path(@order.id), method: :delete, data: { confirm: "Are you sure you want to cancel this order?" }, class: "btn btn-danger mx-1" %>
  </span>
</nav>

</section>
